@AGENTS.md

## Claude Code
- Imported instructions above are advisory; add project-specific rules below.
- Always verify data changes with `python update_zen_prices.py --dry-run --output docs/models.json` before writing, then `python3 -m json.tool docs/models.json`.